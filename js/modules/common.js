define(["jquery"], function($) {

  return  AREA_WIDTH = 12,
          AREA_HEIGHT = 12,
          TIMESTAP = 500,
          PIECE_COLOR = "red",
          BG_COLOR = $("body").css("background-color"),
          RESERVED = "reserved";
});
